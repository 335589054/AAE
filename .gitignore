# ---------------- 通用 ----------------
.DS_Store
Thumbs.db
*.log
.idea/
*.iml
.vscode/

# ---------------- Windows 版（windows/） ----------------
windows/node_modules/
windows/dist/
windows/dist-electron/
windows/release/
windows/tools/
windows/cache/
windows/.selftest.cjs

# 版权资源：Sample/ 是解包出来的 Arcaea 安装包内容（700MB+），不随仓库提供。
# 需要跑 Windows 版自检时请自备：把你合法拥有的安装包解包后放到 windows/Sample/。
windows/Sample/

# ---------------- Android 版（android/） ----------------
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/captures/
.cxx/

# 说明：android/keystore/release.p12 与 android/app/src/main/assets/auto-sign.p12
# 必须随仓库提交——签名密钥要保持固定，否则已安装的包无法覆盖更新，因此不在此忽略。

# 参考用的单曲数据模板（格式已在 android 自检里以等价样例固化）
android/songdata..json

# 内置 ffmpeg 静态二进制（~82MB，不随仓库提交；如需启用请自行放回
# android/app/src/main/jniLibs/arm64-v8a/libffmpegnative.so）
android/app/src/main/jniLibs/

# 会话临时 / 诊断产物
android/_extracted/
android/_*.ps1
android/everythingbk.zip
.trae/

# 谱面 zip 样本（体积较大，仅本地调试用，不随仓库提交）
zipsample/
